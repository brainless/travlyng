export interface Place {
  id: number;
  name: string;
  description?: string;
  location?: string;
}

export interface Accommodation {
  id: number;
  name: string;
  description?: string;
  location?: string;
}

export interface Restaurant {
  id: number;
  name: string;
  description?: string;
  location?: string;
}
